export const metadata = {
  title: 'File Hashing - HashVault',
  description: 'Generate cryptographic hashes from local files.',
};

export default function FileLayout({ children }) {
  return <>{children}</>;
}
