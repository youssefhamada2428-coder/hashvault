export const metadata = {
  title: 'Hash History - HashVault',
  description: 'View the comprehensive history of all cryptographic operations.',
};

export default function HistoryLayout({ children }) {
  return <>{children}</>;
}
