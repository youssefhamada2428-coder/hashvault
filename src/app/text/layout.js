export const metadata = {
  title: 'Text Hashing - HashVault',
  description: 'Generate cryptographic hashes from string payloads.',
};

export default function TextLayout({ children }) {
  return <>{children}</>;
}
